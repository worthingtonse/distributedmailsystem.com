# Node.js dependencies
node_modules/
client/node_modules/
server/node_modules/

# Build outputs
dist/
client/dist/
server/dist/

# Security & Logs
.env
.env.bak*
*.env.bak*
SoldCoins.txt
npm-debug.log*

.git/
# Machine-local payment switches (see server/paypal-mode.txt.example)
server/paypal-mode.txt

# Live data - customer PII and coin inventory, never commit
users.csv
influencer_payments.csv
server/analytics_events.csv
server/affiliate_sales.csv
server/issued_wallets.json
server/redeemed_orders.json
server/wallet_stock_warnings.json
qmail_preconfigured_wallets/

# Editor swap files
*.swp

# Pre-minted locker-key pools + issued ledger - live coin keys, never commit
server/inventory/
server/subscriptions.json
server/subscription_ledger.json

# Deploy rollback snapshots
server/dist.bak-*

# Waitlist + bug-dismissal state (contains emails/PII)
server/waitlist.json
server/bug_dismissed.json
